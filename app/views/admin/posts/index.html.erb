    <div class="container">
    <div class="row">
      <div class="admin_post_index_header">
      <h3>投稿一覧</h3>
    </div>
    </div>
  <div class="row">
    <div class="col-1"></div>
    <div class="col-10">
      <div class="admin_post_index_table">
      <table class="table">
        <thead class="thead-dark">
          <tr>
          <th>タイトル</th>
          <th>投稿者</th>
          <th>投稿ジャンル</th>
          <th>作成日時</th>
          <th>投稿詳細</th>
        </tr>
        </thead>
        <tbody>
          <% @posts.each do |post| %>
          <tr>
            <td><%= post.title %></td>
            <td><%= post.member.nickname %></td>
            <td><%= post.post_genre.name %></td>
            <td><%= post.created_at %></td>
            <td><%= link_to "投稿詳細", admin_post_path(post) %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="col-1"></div>

  </div>
</div>
</div>