<h4>コメントを投稿する</h4>
<%= form_for([@post, @post_comment], url:public_post_post_comments_path(@post), remote: true, html: {method: 'POST'}) do |form| %>
        <div class="field">
          <%= form.text_area :comment, size:"78" %>
        </div>
        <div class="submit_button">
          <%= form.submit "コメント作成", class:"btn btn-primary"%>
        </div>
      <% end %>