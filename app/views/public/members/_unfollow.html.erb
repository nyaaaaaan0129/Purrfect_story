<%= form_for(current_member.active_relationships.find_by(followed_id: @member.id), url: public_relationship_path(@member.id), html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-danger" %>
<% end %>